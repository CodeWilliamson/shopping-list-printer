#include <Arduino.h>

// MVP placeholder for BLE client implementation.
// Next step: discover writable characteristic and stream chunked ESC/POS bytes.

bool printerBleSend(const uint8_t* data, size_t length) {
  (void)data;
  (void)length;
  return false;
}
